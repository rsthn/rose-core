#!rose :deploy

ECHO {clr 92}Running tests ...{clr 0}
    RUN test\\run.bat
    ECHO

ECHO {clr 92}Running expression tests ...{clr 0}
    ;; Uses the in-repo runner, not `rose test` -- the latter loads rose-core from rose-cli's
    ;; vendor directory and would validate the last published release instead of this tree.
    RUN php test/run-fn.php test/test-all.fn
    ECHO

ECHO {clr 92}* Tests completed{clr 0}
